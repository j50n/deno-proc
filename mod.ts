/**
 * Points to legacy library for now to avoid breaking existing code.
 * This will be pointed to `mod3.ts` on the `proc` 1.0 release.
 * The legacy library will continue to be updated regularly until
 * things start breaking, at which point it will be removed.
 */
export * from "./mod1.ts";
